- name: install docker to workers
  hosts: servers
  become: true
  roles:
    - docker-installation

- name: install docker to manager
  hosts: localhost
  become: true
  roles:
    - docker-installation